[project]
name = "see-core"
version = "0.1.0"
description = "SEE CORE deterministic engine (skeleton)"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
see-core = "see_core.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
